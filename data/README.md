# Данные

`train.parquet` — исходный файл соревнования. Его не следует перезаписывать или дополнять в процессе экспериментов.

Все производные таблицы и временные срезы сохраняются только в `artifacts/`.
